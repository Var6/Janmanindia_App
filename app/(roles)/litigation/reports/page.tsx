import EodReportsPanel from "@/components/reports/EodReportsPanel";

export default function LitigationReportsPage() {
  return (
    <EodReportsPanel
      title="EOD Reports & Invoices"
      subtitle="Log your day, court travel and case-related expenses. HR verifies, then your district's head lawyer (or director) approves the invoice."
    />
  );
}
