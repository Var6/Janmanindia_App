// Directors manage projects + funding too. The /api/projects endpoints already
// allow director (create / allocate funds / edit phases); this exposes the same
// management UI under the director workspace.
export { default } from "@/app/(roles)/superadmin/projects/page";
